// jest-dom adds custom Jest matchers for asserting on DOM nodes.
// It allows you to do things like expect(element).toHaveTextContent(/react/i)
import '@testing-library/jest-dom';

// If you have other global configurations or setups, they would go here.
// For example, setting up a global mock for a frequently used library, 
// configuring Enzyme (if you use it alongside React Testing Library), etc.